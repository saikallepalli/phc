# Copy to `.env` and fill in real values. Commit THIS file, never `.env`.
# Add `.env` to both .gitignore and .dockerignore.

DB_HOST=184.168.113.203
DB_PORT=3306
DB_NAME=phcdashboard
DB_USER=phcdashboarduser
DB_PASS=replace_me

# Set by Jenkins to the build number; defaults to "local" for manual runs.
APP_TAG=local
